# Leave empty to use the Vite dev proxy (/api -> localhost:8000) — plug & play in development.
# Set explicitly for production builds or when the API runs on another host.
# VITE_API_URL=http://localhost:8000
