# Operator branding

Replace the logo used on the Operator home screen:

1. Prefer file: `operator-logo.png` (recommended for production artwork)
2. Fallback file: `operator-logo.svg` (current placeholder)

Keep the **same filenames**. After replacing, rebuild/redeploy the frontend.
